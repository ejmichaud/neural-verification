
def f(s,t):
    a = 0;
    ys = []
    for i in range(10):
        b = s[i]; c = t[i];
        next_a = (not a and b and c) or (a and not b and not c) or (a and not b and c) or (a and b and not c)
        a = next_a;
        y = a
        ys.append(y)
    return ys
    